#include <stdio.h>

// standard input/output

int main() {
    // code goes here

    // the int keyword indicates that the main function will return an integer - a simple number
    // if we want to say our code was run successfully, we will return the number 0, a number greater than 0 means that the program we wrote failed

    printf("Hello World!");

    return 0; // every statement in c must end with a semicolon, so that the compiler knows that a new statement has started
}

// to compile and run

// gcc c_file_name -o executable_name
// .\executable_name.exe


